let username: string | number = 'Alice';

username = 'Bob';
username = 4;

// username = false;

const numbers: (string | number)[] = [];

numbers.push(4);
numbers.push('hello');

const myVar = numbers.pop();
